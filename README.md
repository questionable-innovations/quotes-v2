# quotes-v2

Quote book platform.

- **backend/** — Rust (axum + SQLite/Turso) API: auth, books, quotes, members, invites, share links, attachments
- **quota-flutter/** — Flutter mobile client

See each directory's README for setup. `docker-compose.yml` runs the backend.
